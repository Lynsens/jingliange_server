{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Main",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "check-go-version": "false",
            "program": "${workspaceFolder}/cmd",
            // "args": ["--config=conf/conf.toml"]
            "cwd": "${workspaceFolder}", // Set the working directory to your workspace folder

        }
    ]
}